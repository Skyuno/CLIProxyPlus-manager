[project]
name = "cliproxyplus-manager"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "aiohttp>=3.12.0",
    "pyyaml>=6.0",
    "requests>=2.32.5",
]
